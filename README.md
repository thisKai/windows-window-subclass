# windows-window-subclass
A safe wrapper around win32 window subclassing

* Extend the window procedure (WndProc) of win32 windows
* Works with any windowing crate that uses raw-window-handle (winit, sdl2, etc...)